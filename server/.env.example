# Backend Environment Variables (for Render/Railway/Fly.io)
# Copy these to your backend hosting platform's Environment Variables section

# Supabase Service Role Key (NEVER expose this to frontend)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
SUPABASE_URL=your_supabase_project_url

# Google Earth Engine Configuration (RECOMMENDED - for real satellite imagery)
# 1. Create a Google Cloud project: https://console.cloud.google.com
# 2. Enable Earth Engine API: https://console.cloud.google.com/apis/library/earthengine.googleapis.com
# 3. Create a service account and download JSON key
# 4. Set GEE_PROJECT_ID to your project ID (e.g., "my-project-123456")
# 5. Set GEE_SERVICE_ACCOUNT_KEY to the JSON key contents OR path to the key file
GEE_PROJECT_ID=your_google_cloud_project_id
GEE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"...","private_key":"..."}
USE_GEE=true

# Sentinel Hub Configuration (alternative to GEE)
SENTINEL_CLIENT_ID=your_sentinel_client_id
SENTINEL_CLIENT_SECRET=your_sentinel_client_secret
USE_SENTINEL_HUB=false

# Mock/Mapbox fallback (used when both GEE and Sentinel are disabled)
USE_MOCK_SATELLITE=false

# Mapbox Access Token (for fallback/mock satellite service)
MAPBOX_ACCESS_TOKEN=your_mapbox_access_token

# OpenAI API Key (for manifesto analysis)
OPENAI_API_KEY=your_openai_api_key

# Frontend URL (for CORS - set to your Vercel deployment URL)
# Example: https://mtaji-tracker.vercel.app
FRONTEND_URL=http://localhost:5176

# Server Configuration
PORT=3001
NODE_ENV=production
